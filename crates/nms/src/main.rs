//! NMS server entry point (Go cmd/app/main.go). Written by the orchestrator
//! after services/api land; stub compiles for now.

fn main() {
    eprintln!("nms-server: wiring lands after service/api modules are implemented");
    std::process::exit(1);
}
