//! WinRM plugin binary (Go plugin-code/winrm/main.go). Same stdin/stdout JSON
//! protocol. Implemented by the winrm-plugin agent.

fn main() {
    eprintln!("winrm plugin: stub — implemented by winrm-plugin agent");
    std::process::exit(1);
}
